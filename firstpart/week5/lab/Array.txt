ค่าในarrayเรียกว่า element

การสร้างarrayแบบconstructor
ไม่สามารถสร้างarrayด้วยเลขตัวเดียวได้ เพราะมันจะหมายถึงการกำหนดขนาดarray

แต่ถ้าใช้ Array.of() จะเป็นvalue เช่น Array.of(10) = [10]


spread = กระจายแยกออกมาทีละตัว
rest = ยุบมารวมกันเป็นarrayตัวเดียว